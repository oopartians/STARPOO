SetShipSpeed(enemyShips.length);
if(enemyShips.length.a > 0){
}
else{
	// Shoot();
}
var pos = GetPos();
// log('x : '+pos.x+', y : '+pos.y);
// log(allyShips[0].x);
// log(allyShips.length);